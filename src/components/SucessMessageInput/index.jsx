import React from "react";
import './styles.css'
const SucessMessageInput = ({children}) => {
    return(
        <span className="sucess-message">{children}</span>
    );
};
export default SucessMessageInput;